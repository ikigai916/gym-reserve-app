<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ジム予約管理システム</title>
    <link rel="stylesheet" href="/css/style.css">
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
</head>
<body class="bg-gray-50">
    <div x-data="app()" x-init="init()">
        <!-- ヘッダー -->
        <header class="bg-gray-800 text-white py-4">
            <div class="container mx-auto px-4">
                <h1 class="text-2xl font-bold">🏋️ ジム予約管理システム</h1>
            </div>
        </header>

        <!-- メインコンテンツ -->
        <main class="container mx-auto px-4 py-8">
            <!-- タブナビゲーション -->
            <div class="flex space-x-4 border-b mb-6">
                <button 
                    @click="activeTab = 'reserve'"
                    :class="activeTab === 'reserve' ? 'border-b-2 border-blue-500' : ''"
                    class="px-4 py-2 font-medium"
                >
                    予約する
                </button>
                <button 
                    @click="activeTab = 'my-reservations'"
                    :class="activeTab === 'my-reservations' ? 'border-b-2 border-blue-500' : ''"
                    class="px-4 py-2 font-medium"
                >
                    マイ予約
                </button>
                <button 
                    @click="activeTab = 'mypage'"
                    :class="activeTab === 'mypage' ? 'border-b-2 border-blue-500' : ''"
                    class="px-4 py-2 font-medium"
                >
                    マイページ
                </button>
            </div>

            <!-- エラーメッセージ -->
            <div x-show="error" x-text="error" class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4"></div>

            <!-- 成功メッセージ -->
            <div x-show="success" x-text="success" class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4"></div>

            <!-- 予約するタブ -->
            <div x-show="activeTab === 'reserve'" class="bg-white rounded-lg shadow p-6">
                <h2 class="text-xl font-bold mb-4">予約する</h2>
                <!-- TODO: 予約フォームを実装 -->
            </div>

            <!-- マイ予約タブ -->
            <div x-show="activeTab === 'my-reservations'" class="bg-white rounded-lg shadow p-6">
                <h2 class="text-xl font-bold mb-4">マイ予約</h2>
                <!-- TODO: 予約一覧を実装 -->
            </div>

            <!-- マイページタブ -->
            <div x-show="activeTab === 'mypage'" class="bg-white rounded-lg shadow p-6">
                <h2 class="text-xl font-bold mb-4">マイページ</h2>
                <!-- TODO: ユーザー情報編集フォームを実装 -->
            </div>
        </main>
    </div>

    <script src="/js/utils.js"></script>
    <script src="/js/app.js"></script>
</body>
</html>

