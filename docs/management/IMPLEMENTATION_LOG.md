# 実装ログ (IMPLEMENTATION LOG)

## 2025-12-28: トレーニーダッシュボードの表示ロジック修正 (Issue #41)

### 変更の背景
- **課題**: トレーナー側では予約が複数確認できるが、トレーニー側ではダッシュボードに予約が表示されない、あるいは現在時刻に近い予約が消えてしまう問題が発生した。
- **原因**: `upcomingReservations` ゲッターにおいて、開始時刻（`startTime`）が現在時刻（`now`）より後のもののみをフィルタリングしていたため、現在実施中のセッションや、時刻のパース精度の影響で直近の予約が表示されなくなっていた。

### 主要な変更点
1. **ダッシュボード表示ロジックの改善 (app/static/index.html)**:
   - `upcomingReservations` において、終了時刻（`endTime`）が現在時刻以降のものを表示対象とするように変更。これにより、実施中のセッションもカードとして表示され続ける。
   - 時刻比較の際、当日予約が正しく判定されるよう日付も含めた正確な `Date` オブジェクトで比較を行う。

---

## 2025-12-28: トレーニー予約表示の拡張と稼働枠重複登録防止の実装 (Issue #39, #40)

### 変更の背景
- **課題**: トレーニーが次回のセッションしかダッシュボードで確認できず、全予約状況の把握がしにくかった（Issue #39）。
- **課題**: トレーナーが同じ時間に稼働枠を重複して登録できてしまい、管理上の混乱を招く可能性があった（Issue #40）。

### 主要な変更点
1. **トレーニー向けダッシュボードの改善 (app/static/index.html)**:
   - 「次回のセッション」カードを「今後のセッション予定」リスト表示に拡張。
   - 未来のすべての予約（`upcomingReservations`）を時系列で表示し、直近の予約を強調表示するデザインに変更。
   - Alpine.js に `upcomingReservations` ゲッターを追加。
2. **稼働枠重複登録防止の実装 (app/api/endpoints/availabilities.py)**:
   - `create_availabilities` エンドポイントにて、登録時に既存の枠を確認するロジックを強化。
   - Firestore から取得した既存枠と新規登録枠を UTC タイムスタンプで比較するようにし、型の不整合やタイムゾーンの違いによる重複見落としを防止。
   - 重複している枠はスキップし、新規の枠のみをバッチ処理で登録するように修正。

---

## 2025-12-28: 予約キャンセルエラーの完全解消と本番デプロイ

### 変更の背景
- **バグ**: 本番環境およびローカルで予約キャンセル時に 500 Internal Server Error が発生していた。
- **原因**: 
    1. Firestore トランザクション内で、書き込み（update）の後に読み取り（get）を行っていた（Firestore の制約違反）。
    2. 旧形式のデータで `date` フィールドが `datetime` オブジェクトとして保存されている場合に文字列操作でエラーが発生していた。

### 主要な変更点
1. **トランザクション順序の修正 (app/api/endpoints/reservations.py)**:
   - `cancel_reservation` 内のトランザクションで、稼働枠の取得（読み取り）を先に行い、予約ステータスの更新と稼働枠の解放（書き込み）を後に行うよう順序を修正。
2. **期限チェックの堅牢化 (app/api/endpoints/reservations.py)**:
   - `check_deadline` 関数において、入力が `datetime` オブジェクト、`Timestamp`、または文字列のいずれであっても安全に処理できるようロジックを強化。
3. **データパースの安全向上**:
   - 旧データの救済処理において、`date` がオブジェクトの場合の文字列変換や、`courseMinutes` の整数変換などをより安全に行うよう修正。
4. **スキーマの柔軟化 (app/schemas/reservation.py)**:
   - `ReservationResponse` のフィールドを `Optional` にし、不完全なデータでも一覧表示を継続可能にした。
5. **デプロイ**:
   - `fix-cancel-logic-ordering` ブランチで検証後、`main` にマージしてプッシュ。GitHub Actions による本番デプロイを実施。

---

## 2025-12-28: Cloud Run 起動エラーの修正（徹底調査）

### 変更の背景
- **バグ**: Cloud Run へのデプロイ時に `The user-provided container failed to start and listen on the port` というエラーが発生し、起動に失敗していた。

### 主要な変更点
1. **トランザクション処理の修正 (バックエンド)**:
   - `app/api/endpoints/reservations.py` 内の Firestore トランザクション処理で、サポートされていない `stream()` を `get(transaction=transaction)` に修正。
2. **起動ロギングの強化 (バックエンド)**:
   - `app/main.py` の冒頭でロギングを初期化し、モジュールのインポート過程を詳細に記録するように変更。エラー発生時にスタックトレースを捕捉可能にした。
3. **Dockerfile の最適化**:
   - `CMD` を簡素化し、`uvicorn` を直接実行するように変更。また、環境変数 `PORT` のデフォルト値を明示的に設定。
4. **不要ファイルの除去**:
   - リポジトリに混入した `__pycache__` を削除。

---

## 2025-12-28: 予約一覧UIの改善とキャンセル機能の堅牢化

### 変更の背景
- **課題**: トレーニーの予約一覧が単調で、過去のセッションと未来の予約が区別しにくい。
- **バグ**: 予約のキャンセル時にエラーが発生することがあり、また関連する稼働枠の解放がタイムゾーンの影響で失敗する可能性があった。

### 主要な変更点
1. **予約一覧UIの刷新 (フロントエンド)**:
   - 予約カードにステータスタグ（確定、実施中、終了、取消済）を追加。
   - 終了したセッションやキャンセル済みのセッションをグレーアウト（モノクロ化・透過）し、視覚的に区別。
   - 実施中のセッションにはアニメーション付きのタグを表示。
2. **キャンセルロジックの改善 (バックエンド)**:
   - `Reservation` ドキュメント作成時に、`startAt` と `endAt` を Firestore Timestamp 形式で保存するように変更。
   - `cancel_reservation` エンドポイントにおいて、保存された Timestamp を使用して稼働枠を検索するように修正。

### 技術的決定
- **Timestamp優先**: Firestore の比較演算において最も信頼性の高い Timestamp 型をドキュメント間に持たせることで、キャンセル時の整合性を担保。
- **状態の事前計算**: フロントエンド（Alpine.js）側で `isPast`, `isOngoing` を計算し、宣言的にスタイルを制御。

---

## 2025-12-28: UI/UX改善 Phase 1 の実装

### 変更の背景
- **課題**: `alert()` や `confirm()` などのネイティブUIによる体験の低下、情報の視認性不足、トレーナーの運用負荷。
- **目的**: プロフェッショナルな外観と、直感的な操作性の実現。

### 主要な変更点
1. **フロントエンド基盤の強化**:
   - **通知システム**: カスタムトースト通知を実装し、操作結果（成功・失敗）を非同期で表示。
   - **カスタムモーダル**: `confirm()` を置き換え、予約や削除の確認を美しいダイアログで実施。
   - **ローディング・スケルトン**: 通信中の状態を可視化。スケルトンアニメーションにより、データの読み込み待ちを自然に。
2. **ロール別ダッシュボードの導入**:
   - **トレーナー**: 週間の予約統計（セッション数、合計時間）を表示。
   - **トレーニー**: 直近3日間の空き枠をトップに表示し、カレンダー操作なしで即座に予約可能に。
3. **予約UIの高度化**:
   - **ステータスバッジ**: 「予約済」「時間不足」などの状態を色とラベルで判別可能に。
   - **時間不足判定**: 選択したコース時間（60-180分）に対して連続した空きがない場合、理由とともに非活性化。
   - **日付ショートカット**: 「今日」「明日」「日曜」へのクイックジャンプ。
   - **空き枠フィルタ**: 予約可能な枠だけを絞り込むトグルを追加。
4. **トレーナー運用効率化**:
   - **前週コピー機能**: バックエンドに `/api/availabilities/copy-last-week` を追加。ワンクリックで先週と同じシフトを登録可能に。
5. **アクセシビリティとモバイル対応**:
   - 期限切れのキャンセルボタンを自動非表示にし、理由を表示。
   - フォーカスリングの視認性向上と、モバイルでのレイアウト調整。

### 技術的決定
- **Alpine.js**: 状態管理をさらに活用し、通知やモーダルの開閉、並列データ取得を実現。
- **Firestore Batch**: 稼働枠のコピー処理において、アトミックな書き込みを保証。

---

## 2025-12-28: 予約取得エラーの解消とトレーニーへの次回予約表示

### 変更の背景
- **バグ**: トレーニーがログインした際、「予約情報の取得に失敗しました」というエラーが発生。Firestore の `order_by` クエリに対して複合インデックスが不足していた。
- **課題**: トレーニー画面に「次回のセッション」カードが表示されないロジック上の制限があった。

### 主要な変更点
1. **インデックスエラーの回避 (バックエンド)**:
   - `app/api/endpoints/reservations.py` の `get_reservations` から `order_by("createdAt", ...)` を削除。
   - すでにフロントエンド側で並び替えロジックを実装済みのため、インデックス作成を待たずに正常にデータを取得できるようにした。
2. **トレーニー向け次回予約表示 (フロントエンド)**:
   - `app/static/index.html` の `nextReservation` getter からトレーナー限定のロールチェックを削除。
   - トレーニーでも、自身が入れた一番近い未来の予約が表示されるようにした。

### 技術的決定
- Firestore のインデックス管理の煩雑さを避けるため、ソート処理をフロントエンドに完全に委ねる設計とした。

---

## 2025-12-28: availabilities.py の SyntaxError 修正

### 変更の背景
- Cloud Run でのデプロイが継続して失敗しており、ログを確認したところ `app/api/endpoints/availabilities.py` の 108 行目付近で `SyntaxError: invalid syntax` が発生していた。

### 主要な変更点
1. **インデントの修正**:
   - `delete_availability` 関数内において、`doc_ref.delete()` および `return` 処理、そして `except` ブロックが `if data["isBooked"]` の中に誤ってインデントされていたのを修正。

### 技術的決定
- 単純な記述ミスであったため、修正後に即座にプッシュし、デプロイの正常動作を確認。

---

## 2025-12-28: Cloud Run 起動エラーの徹底修正（パッケージ構成と依存関係の修正）

### 変更の背景
- 前回の修正でも Cloud Run での起動失敗が継続していたため、より広範な原因（依存パッケージ不足、パッケージ構成不備、Dockerfileの干渉）を特定し、修正を実施。

### 主要な変更点
1. **パッケージ認識の改善**:
   - `app` およびその全サブディレクトリ（`api`, `core`, `schemas`, `endpoints`）に `__init__.py` を追加。
   - これにより、`uvicorn` によるインポートが確実に動作するようにした。
2. **不足パッケージの追加**:
   - `requirements.txt` に `aiofiles` を追加。
   - FastAPI の `StaticFiles` および `FileResponse` の非同期処理には `aiofiles` が必須であり、これがないと起動時または初回アクセス時にエラーが発生する可能性がある。
3. **Dockerfile の簡素化と最適化**:
   - Cloud Run では不要（または干渉の原因）な `HEALTHCHECK` および `EXPOSE` 命令を削除。
   - `COPY` 戦略を簡素化し、`PYTHONPATH` を明示的に設定。
   - `CMD` をより堅牢な形式に調整。
4. **ロギングの強化**:
   - `app/main.py` の冒頭で `logging.basicConfig` を呼び出し、`sys.stdout` への出力を明示。
   - アプリケーション起動プロセスの開始を即座にログ出力するようにし、デバッグ性を向上。

### 技術分決定
- インフラ側（Cloud Run）が期待するコンテナ挙動（$PORT へのバインドとヘルスチェック）を阻害しないよう、Dockerfile を最小構成にした。
- Python パッケージとしての整合性と、FastAPI の静的ファイル配信に必要な依存関係を網羅。

---

## 2025-12-28: Cloud Run 起動エラーの再修正（Firestore遅延初期化と環境変数対応）

### 変更の背景
- 前回の修正後も、Cloud Run でコンテナが 8080 ポートでリッスンを開始できずに起動失敗するエラーが継続。
- アプリケーション起動時のインポートフェーズで Firestore クライアントの初期化に失敗し、プロセスがクラッシュしている可能性が高い。

### 主要な変更点
1. **Firestore クライアントの遅延初期化 (Issue #31 関連)**:
   - `app/core/database.py` において、モジュール読み込み時の即時初期化を廃止。
   - `FirestoreProxy` クラスを導入し、実際にデータベース操作が必要になったタイミングで初めて `firestore.Client()` を呼ぶように変更。
   - これにより、Firestore 接続に問題がある場合でもサーバー自体は起動し、Cloud Run のヘルスチェックを通過できるようになる。
2. **Dockerfile の改善**:
   - `CMD` 命令を `sh -c` 経由に変更し、Cloud Run から渡される `$PORT` 環境変数を使用するように修正。
   - `PYTHONPATH` 環境変数を明示的に設定し、モジュールのインポートを確実に。
3. **詳細なロギング**:
   - `app/main.py` および `app/core/database.py` に起動時ログを追加し、失敗箇所を特定しやすくした。

### 技術的決定
- サーバー起動時の外部依存関係（Firestore）への即時接続を避ける「遅延初期化（Lazy Initialization）」を採用し、システムの耐障害性と起動速度を向上。
- インフラ（Cloud Run）のベストプラクティスに基づき、動的なポート割り当てに対応。

---

## 2025-12-27: Cloud Run 起動エラーの修正

### 変更の背景
- 本番環境（Cloud Run）へのデプロイ時に `container failed to start and listen on the port 8080` エラーが発生。
- 原因調査の結果、`app/main.py` 内で既に削除済みのレガシー関数（`get_reservations_legacy`, `create_reservation_legacy`）をインポートしようとして `ImportError` が発生し、アプリケーションの起動が中断されていたことが判明。

### 主要な変更点
1. **`app/main.py` のクリーンアップ**:
   - 存在しないエンドポイントへのインポートとルーティング処理を削除。
   - 静的ファイルの配信設定（`/assets`, `/`, `/index.html`, `/login.html`）は維持。

### 技術的決定
- インフラ（Cloud Run）の設定自体には問題がなかったため、アプリケーションコードの整合性を修正することで対応。

---

## 2025-12-27: トレーニー用UI強化と稼働枠管理の最適化 (Issue #37, #38)

### 変更の背景
- **トレーニー要望**: 「次の予約がいつか、誰がトレーナーか」を明確にしたい。また、予約時に特定のトレーナーを選びたい。
- **管理上の課題**: 稼働枠を誤って二重に生成してしまうことがあり、また削除した際に画面の反映が遅れることがあった。

### 主要な変更点
1. **トレーニー用UIの強化 (Issue #37)**:
   - **次回予約カード**: トレーニー画面トップにも、最も近い確定セッションと担当トレーナー名を表示。
   - **トレーナー選択フィルタ**: 予約フローの最初に「トレーナーを選択」ステップを追加。選択したトレーナーの空き枠のみを表示するようにした。
   - **予約一覧**: 自分の予約リストに担当トレーナー名を明示。
2. **稼働枠管理の改善 (Issue #38)**:
   - **重複登録防止ロジック**: `create_availabilities` エンドポイントにて、バッチ登録前に既存の枠を検索し、同一トレーナー・同一時間の枠が存在する場合は登録をスキップするガードを実装。
   - **削除同期の確実化**: フロントエンドの `deleteSlot` において、APIのレスポンスを待ってからローカルの状態 (`trainerSlots`) を即座に更新するように変更。
3. **API拡張**:
   - `get_users` にてロール別のフィルタリングを可能にし、フロントエンドからトレーナー一覧を効率的に取得できるようにした。

### 技術的決定
- UIの即時反映のため、サーバーからの再読み込みを待つだけでなく、フロントエンド側の配列を直接操作（`filter`）するアプローチを採用し、体感速度を向上。
- 重複チェックは、Firestoreの `Timestamp` 型を `ISO 8601` 文字列に変換して比較することで、精度の高い一致判定を実現。

---

## 2025-12-27: 予約作成バグの修正とトレーナー用UIの強化 (Issue #34, #35)

### 変更の背景
- **バグ**: トレーニーが空き枠を予約しようとしても「既に予約されている」と誤判定されるケースがあった。これはフロントエンド（ブラウザ local/UTC）とバックエンド（サーバー naive datetime）のタイムゾーン不整合が原因。
- **機能要望**: トレーナーが「次がいつ、誰の予約か」を即座に確認したいという要望があった。

### 主要な変更点
1. **タイムゾーン不整合の解消 (Issue #34)**:
   - **スキーマ拡張**: `ReservationCreate` に `startAt` (ISO string) を追加。
   - **フロントエンド**: `availableSlots` の計算時に各スロットの UTC ISO 文字列を保持し、予約時に送信するように変更。
   - **バックエンド**: `startAt` が存在する場合、タイムゾーン（UTC）を考慮してパース・比較を行うように修正。
2. **トレーナー用UIの強化 (Issue #35)**:
   - **「次回のセッション」セクション**: トレーナー画面の最上部に、最も近い確定予約を表示するカードを追加。
   - **ソートロジックの改善**: 
     - トレーナー：予約日時が近い順（昇順）にソート。
     - トレーニー：予約日時が新しい順（降順）にソート。
   - **予約状況サイドバー**: キャンセル済みの予約を薄く表示し、ステータスを判別しやすくした。

### 技術的決定
- 日時情報の受け渡しを naive datetime ではなく、タイムゾーン情報を含む ISO 文字列（ISO 8601）に統一することで、環境に依存しない正確な比較を実現。
- Alpine.js の `computed properties` (getters) を活用し、リアクティブに「次の予約」や「ソート済み一覧」を計算する設計とした。
- プロセス遵守: Issue #34, #35 起票、修正ブランチ作成、PR #36 によるマージを実施。

---

## 2025-12-27: UI/UX：ロール別表示制御と予約枠のグレーアウト実装 (Issue #32)

### 変更の背景
- トレーナーとトレーニーで必要な機能が混在しており、混乱を招く可能性があった。
- 予約画面において、空き枠のみを表示するのではなく、全時間帯を可視化した上で「予約不可」な理由を明確にする（グレーアウト）ことで、ユーザーの選択をスムーズにする必要があった。

### 主要な変更点
1. **ロール別表示制御 (フロントエンド)**:
   - トレーナー：稼働枠管理パネルのみを表示し、予約フローを非表示化。
   - トレーニー：予約フローのみを表示し、稼働枠管理パネルを非表示化。
2. **予約スロットのステータス管理**:
   - その日の全登録スロットを表示。
   - ステータスを `available` (可能), `booked` (予約済), `insufficient` (時間不足) の3段階で管理するロジックを実装。
   - 複数トレーナーが存在する場合でも、同一トレーナーで連続した時間が確保できるか判定するようにロジックを強化。
3. **予約画面のUI改善**:
   - `booked` スロット：グレー背景 + 「済」ラベル。
   - `insufficient` スロット：薄い表示 + 非活性化。
4. **サイドバーの表示拡張**:
   - トレーナーが閲覧する場合、予約者の氏名を表示するように追加。
5. **バックエンドのフィルタリング修正**:
   - `get_reservations` において、トレーナーは自分に関連する予約のみを閲覧できるように修正。

### 技術的決定
- Alpine.js の `x-show` と動的なクラスバインディング (`:class`) を活用し、状態に応じた表示の切り替えを効率的に実装。
- スロット判定ロジックをフロントエンドに持たせることで、ユーザーのコース時間選択に即座に反応するシームレスな体験を実現。
- プロセス遵守: Issue #32 起票、修正ブランチ `feature/32-ui-ux-role-refinement` 作成、PR #33 によるマージを実施。

---

## 2025-12-27: 予約状況サイドバーの動的更新と表示修正 (Issue #30)

### 変更の背景
- 予約完了後やキャンセル後に、サイドバーの「予約状況」が自動でリロードされない問題があった。
- 一部の古いデータや不完全なデータにおいて、時間が `undefined` と表示されていた。

### 主要な変更点
1. **確実なデータ再取得**: `confirmReservation` と `cancelReservation` において、`await` を用いて `loadMyReservations()` の完了を待機してから完了通知（alert）を出すように変更。
2. **表示の堅牢化**: HTMLテンプレート内で `res.startTime || '??:??'` のようにフォールバック値を設定し、`undefined` の表示を回避。
3. **エラーハンドリング**: フロントエンドの `fetch` 処理に `try-catch` を追加し、コンソールにエラーを出力するようにした。

### 技術的決定
- ユーザー体験（UX）向上のため、サーバーからの最新レスポンスがUIに反映されたことを確実にしてから完了メッセージを出すフローとした。
- プロセス是正: Issue #30 起票、修正ブランチ作成、PR #31 によるマージを実施。

---

## 2025-12-27: 本番環境での API Fetch エラー修正と運用プロセスの是正 (Issue #28)

### 変更の背景
- 本番環境（Cloud Run）において、APIエンドポイントの末尾スラッシュ欠如により「Failed to fetch」エラーが発生していた。
- 緊急修正時にルール（Issue/ブランチ作成）を逸脱したため、事後にプロセスを遡及適用し履歴を整理。

### 主要な変更点
1. **API呼び出しパスの修正**: `index.html` 内のすべての `fetch` パスに末尾スラッシュを追加（例: `/api/availabilities/`）。
2. **プロセス是正**:
   - Issue #28 起票
   - 修正用ブランチ `fix/28-trailing-slash-fetch-error` 作成
   - PR #29 によるマージ記録の作成

### 技術的決定
- FastAPIのリダイレクト（307）によるFetchエラーを回避するため、リクエストパスをサーバー側の定義に厳密に合わせる方針を徹底。

---

## 2025-12-27: JWT認証と30分単位の予約エンジンの実装 (Issue #26)

### 変更の背景
- プロトタイプ版として、外部依存のないJWT認証基盤を構築。
- ビジネスルールに基づき、30分単位の枠管理と前日24時期限を厳密に実装。

### 主要な変更点
1. **認証基盤**: `app/core/auth.py` にJWT生成・検証ロジックを実装。パスワードは bcrypt でハッシュ化。
2. **稼働枠管理**: `availabilities` コレクションを導入。トレーナーが30分単位で稼働枠を一括登録できるAPIを実装。
3. **予約エンジン**: 
   - 連続スロット確保ロジック（コース時間に応じた複数スロットの占有）。
   - 前日24時までの期限チェックバリデーション。
   - Firestoreトランザクションによるアトミックな予約処理。
4. **フロントエンド**: `Zen Conditioning` のブランドに合わせたミニマルなデザインに刷新。コース選択と30分単位の枠表示に対応。

### 技術的決定
- プロトタイプ速度を優先し、メールアドレス/パスワード認証を独自実装（パスワードはハッシュ化）。
- UIの状態管理には Alpine.js を継続採用し、軽量さを維持。

---

## 2025-12-27: ビジネスルールの詳細化とドキュメント一新

### 変更の背景
- HP（Zen Conditioning / Kukan）の内容に基づき、より実務に即した要件にアップデート。
- 30分単位の予約、前日24時の期限、55分セッション等の重要な制約を定義。

### 主要な定義
1. **予約期限**: 30日前〜1日前24時まで。
2. **稼働管理**: トレーナーが自身の空き枠（Availability）を登録し、それを顧客が予約する方式を採用。
3. **認証方針**: 将来の拡張性を考慮し、ID/パスワードによるセキュアな方式へシフト。

### 技術的決定
- Firestore に `availabilities` コレクションを追加。
- 予約時には、複数の30分スロットを「束」として扱うロジックを実装予定。
